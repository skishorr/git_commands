commands for pushing the codes for 1st time
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/skishorr/git_commands.git
git push -u origin main

commands for pushing the codes from 2nd time

git add .
git commit -m "comments"
git push

extra commands

git branch //will used to fetch list of branches
git branch branchname  //it will create new branch
git checkout branchname //switch between the branches
git branch -d sk //to delete the branch

git push --set-upstream origin dev //pushing updates to new branch, here dev is the new branch
